fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
aiosqlite>=0.20
apscheduler>=3.10,<4
httpx>=0.27
pydantic>=2.6
python-multipart>=0.0.9
tzdata>=2024.1
# For the verified qp library (quant-platform) — INTEGRATION.md §5
pandas>=2.0
numpy>=1.24
# Optional broker SDK (BrokerEngine falls back to the Alpaca REST API via
# httpx when this is not installed):
# alpaca-trade-api>=3.0

# Development / testing
pytest>=8.0
pytest-asyncio>=0.23
